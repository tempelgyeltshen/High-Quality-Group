{
  "name": "high-quality-group-pos",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "npx tsx backend/dev-server.ts",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "node dist/server.cjs",
    "clean": "rm -rf dist backend/dist frontend/dist",
    "lint": "npm run lint --prefix frontend && npm run lint --prefix backend",
    "lint:frontend": "tsc --noEmit --project frontend/tsconfig.json",
    "lint:backend": "tsc --noEmit --project backend/tsconfig.json",
    "test": "npm test --prefix backend"
  },
  "devDependencies": {
    "tsx": "^4.23.13",
    "typescript": "~5.8.2"
  }
}
